import { InjectionToken } from '@angular/core';

export const API_URL = new InjectionToken('apiUrl');
export const SWAGGER_URL = new InjectionToken('swaggerUrl');

export const API_SEGMENT = '/api';
export const SWAGGER_SEGMENT = '/swagger-ui.html';
